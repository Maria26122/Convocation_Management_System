@model Convocation.Entities.RolePermission
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Delete Role Permission";
}
<div class="form-card">
    <h3 class="form-title mb-4 text-danger">Delete Role Permission</h3>
    <p>Are you sure you want to delete this mapping?</p>
    <dl class="row">
        <dt class="col-sm-3">Role</dt>
        <dd class="col-sm-9">@Model.Role?.RoleName</dd>
        <dt class="col-sm-3">Permission</dt>
        <dd class="col-sm-9">@Model.Permission?.PermissionName</dd>
    </dl>
    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="RolePermissionId" />
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </form>
</div>