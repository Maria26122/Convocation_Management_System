@model Convocation.Entities.UserPermission
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Edit User Permission";
}
<div class="form-card">
    <h3 class="form-title mb-4">Edit User Permission</h3>
    <form asp-action="Edit" method="post">
        <input type="hidden" asp-for="UserPermissionId" />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="mb-3">
            <label asp-for="UserAccountId" class="form-label">User</label>
            <select asp-for="UserAccountId" asp-items="ViewBag.UserAccountId" class="form-select"></select>
            <span asp-validation-for="UserAccountId" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="PermissionId" class="form-label">Permission</label>
            <select asp-for="PermissionId" asp-items="ViewBag.PermissionId" class="form-select"></select>
            <span asp-validation-for="PermissionId" class="text-danger"></span>
        </div>
        <button type="submit" class="btn btn-primary">Update</button>
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </form>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}